<%= form_for([@recipe, @recipe.ingredients.build]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :quantity %>
  <%= f.text_field :quantity %>
  <%= f.submit "Create Recipe" %>
<%end%>
